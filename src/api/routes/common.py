from typing import Dict

"""
from ...db.models.boards_model import getBords
from ...db.models.BU_user import getUserId
from ...db.postgres import PostgresDB
from ...schemas.boards_schema import boardId
from ...schemas.users_schema import UserData


def getUserBords(user: UserData, DB: PostgresDB) -> Dict:
    userId = getUserId(user, DB)
    boards = getBords(userId, DB)
    return boards
"""